<?xml version="1.0" encoding="utf-8"?>
<!-- Wide HUD, two launcher rows tall by default. Keep vertical resize enabled
     so old 5x3 One UI placements can be shrunk, but cap growth on hosts that
     honor Android 12+ maxResizeHeight. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
  android:minWidth="320dp"
  android:minHeight="110dp"
  android:minResizeWidth="250dp"
  android:minResizeHeight="110dp"
  android:maxResizeHeight="180dp"
  android:targetCellWidth="5"
  android:targetCellHeight="2"
  android:updatePeriodMillis="0"
  android:initialLayout="@layout/scouter_widget_medium"
  android:resizeMode="horizontal|vertical"
  android:widgetCategory="home_screen" />
